# Deprecated — use the "Release SDK" workflow instead.
name: Manual Release (deprecated - use Release SDK)

on:
  workflow_dispatch:
    inputs:
      note:
        description: 'Use "Release SDK" workflow instead.'
        required: false

jobs:
  deprecated:
    runs-on: ubuntu-latest
    steps:
      - name: Redirect notice
        run: echo "This workflow is deprecated. Please use the Release SDK workflow."
